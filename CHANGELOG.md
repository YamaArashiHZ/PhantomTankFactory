## 更新内容

### 新功能

- **彩色幻影坦克合成（实验性）** — 在图像参数卡片选择「彩色」模式，输出保留原色彩。采用双通道算法：灰度管线确保 Alpha 通道的亮度信息准确，彩色管线保留 RGB 细节
- **拖拽导入图片** — 从文件管理器直接拖放图片到表图/里图预览框即可加载，同时支持效果查看页面拖放。拖入文件时全屏暗色遮罩自动在预览框位置挖孔高亮
- **对比度 & 饱和度调节** — 图像参数卡片新增两个滑条，在原图阶段调整对比度（-100~100）和饱和度（-100~100），0 为不变
- **黑白/彩色参数独立存储** — 两种模式各自记住亮度、对比度、饱和度设置，切换模式自动恢复对应参数
- **导出进度条** — 生成按钮右侧新增进度条，百分比平滑增长，完成后短暂显示 100%
- **滑块平滑动画** — 切换模式时参数滑块以 easeOutCubic 曲线在 280ms 内平滑过渡到新值

### 优化

- **亮度调节算法重写** — 从断开的两段式公式改为线性插值：正方向向白色渐变、负方向向黑色渐变、0 值保持原样。默认值（50/-50）结果与旧版完全一致
- **效果查看页 UI 改进** — 预览面板增加 2.5% 边距防止高亮边框被遮挡；清除按钮与选择按钮同高，文字及图标改为红色 #FF0000

### 工程

- **自动化测试** — Rust 35 项（覆盖所有图像处理函数 + 彩色合成验证）、前端 21 项（覆盖配置校验全场景）
- **CI 流水线** — PR / 推送时自动运行 `cargo test` + `cargo clippy` + `cargo fmt` + `vue-tsc` + `vitest`；`release.yml` 在构建前加入测试步骤
- **Pre-commit Hooks** — 提交前自动检查 `cargo fmt --check` + `vue-tsc --noEmit`
- **Dependabot** — 每周自动检查 npm / cargo 依赖小版本更新（忽略大版本）
- **PR 模板** — 标准化 PR 描述格式
- **CI 徽章** — README 顶部显示 CI 状态
